# devenv-config-files
Devenv config files for the different projects i work

## Salesforce
This configuration contains the necesary files to work in saleforce projects.
I contains `Java 21`, `Node 20` and `sfdx-nix` as the main software.
